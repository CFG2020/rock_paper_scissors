<html>
<head>
  <title>Page Title</title>
  <meta charset="UTF-8"/>
</head>
<body>
  <script>
  const choices = ["rock", "paper", "scissors"];
//let the computer randomly choose one of the 3 options
function computerPLay(){
let computerChoice = choices[Math.floor(Math.random() * 3)];
return computerChoice;
}
// prompt a choise form the user. make it case insensitive.
function userPlay() {
let userChoice = prompt("Choose one");
userChoice = userChoice.slice(0, 1).toUpperCase() + userChoice.slice(1).toLowerCase();

while (!choices.include(userChoice)) {
     alert("Wrong item!");
     userPlay();
}
return userChoice;
}
//declare a functions with 2 perameters: computer + user choice
function playround(){
    let userChoice = userPlay();
    let computerChoice = computerPlay();
}

  </script>
</body>
</html>